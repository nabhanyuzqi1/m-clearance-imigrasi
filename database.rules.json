{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "email_config": {
      ".read": "auth != null && auth.token.role == 'officer'",
      ".write": "auth != null && auth.token.role == 'officer'"
    }
  }
}